{
  "name": "@safeway-analytics/ingestion",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "probe": "pnpm exec tsx src/probe.ts",
    "ingest": "pnpm exec tsx src/ingest.ts",
    "ingest:offers": "pnpm exec tsx src/snapshotOffers.ts"
  },
  "dependencies": {
    "@safeway-analytics/shared": "workspace:*",
    "dotenv": "^16.4.7",
    "pg": "^8.13.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/pg": "^8.11.11",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
